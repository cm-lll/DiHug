defaults:
    - _self_
    - general : general_default
    - model : discrete
    - train : train_default
    - dataset : acm_subgraphs


hydra:
  job:
    chdir: True
  run:
    dir: output/${now:%Y-%m-%d}/${now:%H-%M-%S}-${general.name}
